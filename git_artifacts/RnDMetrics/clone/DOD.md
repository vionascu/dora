# Definition of Done

- Source repository remains unchanged.
- Dashboard builds and serves via GitLab Pages.
- Daily snapshots accumulate in SQLite.
- Exports include `latest.json` and `history.json`.
- No secrets exposed in logs, files, or public output.
- Tests pass for metrics and schema validation.
